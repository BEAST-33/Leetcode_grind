// A country has N cities, and a road network that
// provides a bidirectional road between every pair of
// cities. The time taken to go from city i to city j using
// this road network is computed from an array a of
// length N, using the formula ai, aj.
// Each city also has an airport that offers flight
// services to some of the other cities. There are M
// flight routes in all. Each route is bidirectional, and is
// described by cities U, V, and duration of flight(W).
// Find the least possible time in which a person can go
// from city 1 to city N using any sequence of roads and
// flights.
//The input is given from Standard Input in M+2 lines,
// with the following format:
// N M
// A1 A2 . . . AN
// U1 V1 W1
// U2 V2 W2
 // ...
// UM VM WM
class Solution
{
	public:
	 
};
